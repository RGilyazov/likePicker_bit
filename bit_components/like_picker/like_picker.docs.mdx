---
description: A LikePicker component.
---

import { LikePicker } from "./like_picker";

A component that show rating stars

### Component usage

```js
  const [stars, setStars] = useState(0);
  function handleChange(value: number) {
    setStars(value);
  }
  return (
      <LikePicker
        max={5}
        value={stars}
        onChange={handleChange}
        starStyle={{ fontSize: "3em" }}
        descriptionStyle={{ fontSize: "1em" }}
        valueDescription={{
          1: "bad",
          2: "normal",
          3: "good",
          4: "great",
          5: "excellent",
        }}
      />
```
